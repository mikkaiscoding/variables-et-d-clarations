#include <stdio.h>
int main(){
    int v; // int v; c’est une déclaration de variable  variable : case mémoire de l’ordinateur possédant une taille, un nom et une valeur
    v = 3;
    printf("on a v= %i", v);
    return 0;
}